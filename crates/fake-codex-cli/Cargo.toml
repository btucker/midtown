[package]
name = "fake-codex-cli"
version = "0.1.0"
edition = "2024"
description = "Deterministic Codex CLI/app-server simulator for protocol and E2E testing"
license = "MIT"
repository = "https://github.com/btucker/midtown"

[dependencies]
serde_json = "1"
